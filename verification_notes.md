# Operational verification notes

- The customer prediction now resolves with selected coordinates and shows weather and Google route timing factors in the explainable decision trace.
- Fresh browser output at 21:10 IST showed Open-Meteo-derived conditions (28°C, 0 mm rain, 7 km/h wind) and Google route timing (7 minutes) in the live operational panel.
- The lower SIH theory/presentation appendix is absent; the page ends after the map and compact operational panel.
- The browser Maps JavaScript proxy returned a 403 during the preview test. The visual map therefore retains a contained interactive Delhi operations diagram while the server continues to obtain Google route timing; this avoids presenting users with an empty external embed.
- A fresh customer-page reload at 21:15 IST showed the same populated interactive route diagram, current weather and a seven-minute Google route timing result. The browser console contained no new SDK error, although the Maps JavaScript canvas had not replaced the fallback in that session.
- Browser inspection confirms the managed Maps script is injected and exposes `Map`, `TrafficLayer`, `DirectionsService`, `DirectionsRenderer`, `Marker`, and traffic-model constructors. The remaining investigation is therefore confined to map initialization/render state rather than proxy credential availability.
- A direct `new google.maps.Map(...)` construction against the component’s map surface succeeds in the preview. The fallback persistence is therefore being caused by an exception in the route-render callback before the component marks its surface ready.
- After making the route callback non-blocking, a fresh recheck still showed the fallback with current weather and Google timing. The next fix will simplify the optional traffic/directions rendering so the managed Google canvas can become visible independently of advanced route setup.
- The polling-based loader repair succeeded. Fresh customer and postman browser inspections now show Google Maps canvases, labeled delivery markers, zoom controls, route geometry, and Google traffic tiles. The postman route desk correctly renders its active delivery stops on the live map.
- A live customer booking was created through the application at 21:18 IST. The new tracking code appeared immediately in the tracking panel, and active workload rose from two to three, confirming the live booking path is functioning.
- The booking-confirmed and assignment notification intents are durable and correctly marked `provider_required` for both WhatsApp and SMS. The only remaining operational check requires an authenticated postman session to invoke the protected status-update endpoint through its user-facing workflow.
- The protected `delivery.updateStatus` tRPC contract was invoked with the operational authenticated caller at 21:26 IST. It moved the new delivery to `out_for_delivery` and created a durable `out_for_delivery` notification-outbox record with `whatsapp,sms` channels and `provider_required` state; no external message was claimed or sent.
- Samaysetu branding verification passed on both the customer and postman routes: the browser title reads `Samaysetu — Delivery Operations`, the header wordmark reads `SAMAYSETU`, the accessible home label is updated, and the footer uses `SAMAYSETU / DELIVERY OPERATIONS`.
- Preview-access recovery passed on 27 August: restarting the development service restored the originally reported preview URL, and the published Samaysetu domain also responded successfully with the customer workflow and Google Maps canvas available.
- The postman page should be rechecked at mobile width after the shared operational panel loads.
