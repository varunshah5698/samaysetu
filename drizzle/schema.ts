import { index, int, mysqlEnum, mysqlTable, text, timestamp, varchar } from "drizzle-orm/mysql-core";

export const users = mysqlTable("users", { id: int("id").autoincrement().primaryKey(), openId: varchar("openId", { length: 64 }).notNull().unique(), name: text("name"), email: varchar("email", { length: 320 }), loginMethod: varchar("loginMethod", { length: 64 }), role: mysqlEnum("role", ["user", "admin"]).default("user").notNull(), createdAt: timestamp("createdAt").defaultNow().notNull(), updatedAt: timestamp("updatedAt").defaultNow().onUpdateNow().notNull(), lastSignedIn: timestamp("lastSignedIn").defaultNow().notNull() });
export const deliveryFiles = mysqlTable("delivery_files", { id: int("id").autoincrement().primaryKey(), userId: int("userId").notNull(), deliveryId: varchar("deliveryId", { length: 80 }).notNull(), fileName: varchar("fileName", { length: 160 }).notNull(), fileKey: text("fileKey").notNull(), fileUrl: text("fileUrl").notNull(), mimeType: varchar("mimeType", { length: 120 }).notNull(), sizeBytes: int("sizeBytes").notNull(), createdAt: timestamp("createdAt").defaultNow().notNull() }, table => ({ userCreatedIdx: index("delivery_files_user_created_idx").on(table.userId, table.createdAt), deliveryIdx: index("delivery_files_delivery_idx").on(table.deliveryId) }));
export const deliveries = mysqlTable("deliveries", { id: int("id").autoincrement().primaryKey(), trackingCode: varchar("trackingCode", { length: 40 }).notNull().unique(), area: varchar("area", { length: 180 }).notNull(), latitude: varchar("latitude", { length: 32 }).notNull(), longitude: varchar("longitude", { length: 32 }).notNull(), requestedDay: varchar("requestedDay", { length: 16 }).notNull(), preferredTime: varchar("preferredTime", { length: 8 }).notNull(), selectedSlot: varchar("selectedSlot", { length: 32 }).notNull(), predictedScore: int("predictedScore").notNull(), status: mysqlEnum("status", ["booked", "assigned", "out_for_delivery", "delivered", "rescheduled"]).default("booked").notNull(), createdAt: timestamp("createdAt").defaultNow().notNull(), updatedAt: timestamp("updatedAt").defaultNow().onUpdateNow().notNull() }, table => ({ deliveryStatusIdx: index("deliveries_status_idx").on(table.status, table.updatedAt), trackingIdx: index("deliveries_tracking_idx").on(table.trackingCode) }));
export const deliveryEvents = mysqlTable("delivery_events", { id: int("id").autoincrement().primaryKey(), deliveryId: int("deliveryId").notNull(), eventType: varchar("eventType", { length: 40 }).notNull(), note: varchar("note", { length: 320 }).notNull(), createdAt: timestamp("createdAt").defaultNow().notNull() }, table => ({ deliveryEventIdx: index("delivery_events_delivery_idx").on(table.deliveryId, table.createdAt), eventCreatedIdx: index("delivery_events_created_idx").on(table.createdAt) }));
export const deliveryNotificationOutbox = mysqlTable("delivery_notification_outbox", { id: int("id").autoincrement().primaryKey(), deliveryId: int("deliveryId").notNull(), trackingCode: varchar("trackingCode", { length: 40 }).notNull(), eventType: varchar("eventType", { length: 40 }).notNull(), channels: varchar("channels", { length: 64 }).notNull(), providerState: mysqlEnum("providerState", ["provider_required", "queued"]).default("provider_required").notNull(), createdAt: timestamp("createdAt").defaultNow().notNull() }, table => ({ deliveryNotificationIdx: index("delivery_notification_delivery_idx").on(table.deliveryId, table.createdAt), notificationStateIdx: index("delivery_notification_state_idx").on(table.providerState, table.createdAt) }));
export type User = typeof users.$inferSelect; export type InsertUser = typeof users.$inferInsert;
